extends layout

block content
  .container
    h3.bordered Loaded
    hr
    if colls
      ul
      each c in colls
        li
          a(href='/view_named/#{c}') #{c}
    hr
    #{JSON.stringify(colls)}