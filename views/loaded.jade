extends layout

block content
  .container
    h3.bordered Loaded
    hr
    if f
      each item in f
        .clear
        div
          h5 #{item.title}
          div !{item.description}
          span #{item.date_formatted}
          span 
            a(href='#{item.url}') link

//    hr
//    #{JSON.stringify(colls)}